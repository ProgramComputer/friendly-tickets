import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: {
    template: '%s | Agent Workspace',
    default: 'Agent Workspace',
  },
  description: 'Manage support tickets and help customers efficiently.',
} 